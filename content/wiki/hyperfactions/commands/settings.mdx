---
title: Settings Commands
description: Commands for configuring faction settings.
order: 7
---

# Settings Commands

These commands allow Leaders and Officers to configure faction settings like name, description, color, and membership policy.

## Renaming Your Faction

<CommandBlock
  commands={[
    {
      command: "/f rename <name>",
      description: "Change your faction's name",
      permission: "hyperfactions.faction.rename"
    }
  ]}
/>

**Requirements:**
- Must be the Leader
- New name must follow naming rules (3-24 chars, alphanumeric)
- Name must not be taken by another faction

<InfoBox variant="warning">
Renaming is visible to everyone. Previous name references may become outdated.
</InfoBox>

## Setting Description

<CommandBlock
  commands={[
    {
      command: "/f desc <text>",
      description: "Set your faction's description",
      permission: "hyperfactions.faction.description"
    }
  ]}
/>

**Requirements:**
- Officer or Leader role
- Description appears in `/f info`

**Example:**
```
/f desc Warriors of the Northern Realm. Recruiting active players!
```

## Setting Faction Tag

Faction tags are short abbreviations shown in chat next to player names. Tags are configured through the faction settings GUI.

**Access via:**
- `/f settings` → Tag section
- `/f gui` → Settings → Tag

**Requirements:**
- Must be the Leader
- Tag must be 1-5 alphanumeric characters

<InfoBox variant="info">
Tags help identify faction members quickly in chat without displaying the full faction name.
</InfoBox>

## Setting Faction Color

<CommandBlock
  commands={[
    {
      command: "/f color <hex>",
      description: "Set your faction's display color",
      permission: "hyperfactions.faction.color"
    }
  ]}
/>

**Format:** Use hex color codes with or without `#`

**Examples:**
```
/f color #FF5500
/f color FF5500
/f color #00FF00
```

**Color appears in:**
- Territory map
- Faction chat
- GUI elements
- Faction info display

<InfoBox variant="tip" title="Color Codes">
Use a color picker tool to find hex codes. Common colors:
- Red: `#FF0000`
- Blue: `#0000FF`
- Green: `#00FF00`
- Yellow: `#FFFF00`
- Purple: `#800080`
- Orange: `#FF8000`
</InfoBox>

## Opening Membership

<CommandBlock
  commands={[
    {
      command: "/f open",
      description: "Allow anyone to join without invitation",
      permission: "hyperfactions.faction.open"
    }
  ]}
/>

**Requirements:**
- Must be the Leader
- Anyone can join with `/f join FactionName`

<InfoBox variant="info">
Open factions are good for recruiting but provide less control over membership.
</InfoBox>

## Closing Membership

<CommandBlock
  commands={[
    {
      command: "/f close",
      description: "Require invitations to join",
      permission: "hyperfactions.faction.close"
    }
  ]}
/>

**Requirements:**
- Must be the Leader
- Players must be invited to join

This is the default state for new factions.

## Territory Permissions

Territory permissions are managed through the faction settings GUI. Access the settings page to configure what actions are allowed in your faction's claimed territory.

**Access via:**
- `/f settings` → Territory Permissions tab
- `/f gui` → Settings → Territory Permissions

**Available flag categories:**
- **Combat:** `pvp_enabled`, `friendly_fire`, `projectile_damage`, `mob_damage`
- **Building:** `build_allowed`, `block_interact`
- **Interaction:** `door_use`, `container_use`, `bench_use`, `processing_use`, `seat_use`
- **Items:** `item_drop`, `item_pickup`
- **Damage:** `fall_damage`, `environmental_damage`

<InfoBox variant="info" title="For Server Owners">
Territory permission flags require the `hyperfactions.faction.permissions` permission node to modify. Server administrators can lock certain flags via configuration.
</InfoBox>

See [Zones](/plugins/hyperfactions/wiki/concepts/zones) for detailed flag descriptions and defaults.

## Disbanding

<CommandBlock
  commands={[
    {
      command: "/f disband",
      description: "Dissolve your faction permanently",
      permission: "hyperfactions.faction.disband"
    }
  ]}
/>

**Requirements:**
- Must be the Leader
- Requires confirmation

<InfoBox variant="danger" title="Permanent">
Disbanding cannot be undone. All claims are released and all members are removed.
</InfoBox>

## Quick Reference

| Command | Description | Role Required |
|---------|-------------|---------------|
| `/f rename <name>` | Change faction name | Leader |
| `/f desc <text>` | Set description | Officer+ |
| `/f color <hex>` | Set faction color | Officer+ |
| `/f open` | Allow free joining | Leader |
| `/f close` | Require invitations | Leader |
| `/f settings` | Open settings GUI (includes tags and territory flags) | Officer+ |
| `/f disband` | Dissolve faction | Leader |

## Examples

### Complete Faction Setup

1. **Set a description:**
   ```
   /f desc Elite PvP faction. Must have 100+ hours.
   ```

2. **Choose a color:**
   ```
   /f color #FF0000
   ```

3. **Set a tag via the settings GUI:**
   ```
   /f settings
   ```
   Navigate to the Tag section and enter your desired tag (e.g., "ELITE").

4. **Keep it invite-only:**
   ```
   /f close
   ```

### Rebranding

1. **New name:**
   ```
   /f rename Phoenix
   ```

2. **Updated description:**
   ```
   /f desc Risen from the ashes. Stronger than ever.
   ```

3. **New color:**
   ```
   /f color #FF8800
   ```

### Recruiting Phase

```
/f desc Looking for active players! Beginners welcome.
/f open
```

Now anyone can join. Close it after recruiting:

```
/f close
```

## Permission Summary

<PermissionTable
  title="Settings Permissions"
  permissions={[
    {
      permission: "hyperfactions.faction.rename",
      description: "Rename the faction",
      default: "false"
    },
    {
      permission: "hyperfactions.faction.description",
      description: "Set faction description",
      default: "false"
    },
    {
      permission: "hyperfactions.faction.tag",
      description: "Set faction tag",
      default: "false"
    },
    {
      permission: "hyperfactions.faction.color",
      description: "Set faction color",
      default: "false"
    },
    {
      permission: "hyperfactions.faction.open",
      description: "Open faction membership",
      default: "false"
    },
    {
      permission: "hyperfactions.faction.close",
      description: "Close faction membership",
      default: "false"
    },
    {
      permission: "hyperfactions.faction.settings",
      description: "Open faction settings GUI",
      default: "false"
    },
    {
      permission: "hyperfactions.faction.disband",
      description: "Disband faction",
      default: "false"
    }
  ]}
/>

<InfoBox variant="info">
While these permissions allow attempting these actions, the role requirement (Leader/Officer) still applies.
</InfoBox>

## Best Practices

### Choosing a Good Name
- **Memorable** - Easy to type and remember
- **Appropriate** - Follows server rules
- **Unique** - Stands out from others

### Writing a Good Description
- **Informative** - What is your faction about?
- **Recruiting info** - Include requirements if applicable
- **Contact info** - How to reach leadership

### Color Selection
- **Visible** - Avoid colors too close to UI elements
- **Unique** - Try to differ from nearby factions
- **Thematic** - Match your faction's identity

## Related Topics

- [Factions](/plugins/hyperfactions/wiki/concepts/factions) - Faction basics
- [Roles](/plugins/hyperfactions/wiki/concepts/roles) - Who can change settings
- [Basic Commands](/plugins/hyperfactions/wiki/commands/basic) - Creating factions
